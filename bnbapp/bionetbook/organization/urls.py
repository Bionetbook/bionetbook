from django.conf.urls.defaults import patterns, url, include
from organization import views

urlpatterns = patterns("",
#    url(regex=r'^$', view=views.SomeView.as_view(), name='organization_view'),
)


